#include <stdio.h>
#include <math.h>

int main() {
    int base = 2, expoente = 3; // 2^3
    printf("2 elevado a 3: %.2f\n", pow(base, expoente));   // Raiz Quadrada

    printf("Raiz quadrada de 25: %.2f\n", sqrt(25));        // Potencia

    return 0;
}